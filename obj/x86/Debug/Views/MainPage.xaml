<Page x:ConnectionId='1'
    x:Class="The_Paper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:The_Paper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local2="using:The_Paper.Models"
    mc:Ignorable="d"
                        >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView x:ConnectionId='2' Name="splitView" DisplayMode="CompactInline" 
                   OpenPaneLength="100"
                   CompactPaneLength="{Binding Path=ActualWidth,ElementName=Ham}">
            <SplitView.Pane>
                <StackPanel Background="#F5F5F5">
                    <Button x:ConnectionId='3' Background="Transparent" FontSize="20"
                            Name="Ham" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"                  />
                    <ListView x:ConnectionId='4' ItemsSource="{Binding ChannelList}" Background="#F5F5F5"
                              Name="Menu"
                                                                        >
                        <ListView.ItemTemplate>
                            <DataTemplate                            >
                                <StackPanel x:ConnectionId='5' Orientation="Horizontal">
                                    <FontIcon x:ConnectionId='6' FontFamily="Segoe MDL2 Assets"                       Margin="-2,0,0,0"/>
                                    <TextBlock x:ConnectionId='7'                      Margin="15,0,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                    <Frame x:ConnectionId='8' Name="mainFrame"/>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>

