<Page
    x:Class="The_Paper.Views.VideoDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:The_Paper.Views"
    xmlns:local2="using:The_Paper.Controls"
    xmlns:local3="using:The_Paper.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <local2:TabView TabNameList="{Binding TabNameList}">
            <local2:TabView.View>
                <ScrollViewer ViewChanged="ScrollViewer_ViewChanged">
                    <RelativePanel>
                        <local3:CommentControl  Margin="10,10,10,10" Name="cc"/>
                        <TextBlock Text="{Binding LoadCommentStatus}"
                                   RelativePanel.AlignHorizontalCenterWithPanel="True"
                                   FontSize="14"
                                   Margin="0,0,0,10"
                                   Foreground="Gray"
                                   RelativePanel.Below="cc"/>
                    </RelativePanel>
                </ScrollViewer>
            </local2:TabView.View>
        </local2:TabView>
    </Grid>
</Page>
